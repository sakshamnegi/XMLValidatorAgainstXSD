Validator to validate XML against a given XSD schema using lxml and python.

Both schema and XML files need to be in main directory

To change schema file, copy it inside main directory and rename the schema path in main.py
